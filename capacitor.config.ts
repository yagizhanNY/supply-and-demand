import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Supply and Demand App',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
